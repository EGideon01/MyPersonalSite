const funkyRotate = () => {
    const flexBox = document.getElementById("FunkyArea");
    const style = window.getComputedStyle(flexBox)
    const currentDirection = style.getPropertyValue("flex-direction");
    
    if (currentDirection === "row")
        flexBox.style.flexDirection = "column"
    else if (currentDirection === "column")
        flexBox.style.flexDirection = "row"
}
const funkyRotate2 = () => {
    const flexBox = document.getElementById("FunkyArea2");
    const style = window.getComputedStyle(flexBox)
    const currentDirection = style.getPropertyValue("flex-direction");
    
    if (currentDirection === "row")
        flexBox.style.flexDirection = "column"
    else if (currentDirection === "column")
        flexBox.style.flexDirection = "row"
}
const funkyRotate3 = () => {
    const flexBox = document.getElementById("FunkyArea3");
    const style = window.getComputedStyle(flexBox)
    const currentDirection = style.getPropertyValue("flex-direction");
    
    if (currentDirection === "row")
        flexBox.style.flexDirection = "column"
    else if (currentDirection === "column")
        flexBox.style.flexDirection = "row"
}
const funkyRotate4 = () => {
    const flexBox = document.getElementById("FunkyArea4");
    const style = window.getComputedStyle(flexBox)
    const currentDirection = style.getPropertyValue("flex-direction");
    
    if (currentDirection === "row")
        flexBox.style.flexDirection = "column"
    else if (currentDirection === "column")
        flexBox.style.flexDirection = "row"
}
const funkyRotate5 = () => {
    const flexBox = document.getElementById("FunkyArea5");
    const style = window.getComputedStyle(flexBox)
    const currentDirection = style.getPropertyValue("flex-direction");
    
    if (currentDirection === "row")
        flexBox.style.flexDirection = "column"
    else if (currentDirection === "column")
        flexBox.style.flexDirection = "row"
}